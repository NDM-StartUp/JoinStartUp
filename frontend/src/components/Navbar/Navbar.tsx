import {Link} from "react-router-dom";

export const Navbar = () => {
    return (
        <header>
            <nav >
                <Link to="/profile">Profile</Link>
            </nav>
        </header>

    )
}